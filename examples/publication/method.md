Check the receiving environment's declared constraints before applying a prior
method. Run receiver-owned verification after implementation, and treat the
result as adopted only when the receiver observes its use.
